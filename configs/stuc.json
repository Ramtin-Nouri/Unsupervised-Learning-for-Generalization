{
    "gpus": [1],
    "epochs": 50,
    "batch_size": 1,
    "num_workers": 8,
    "use_joints": false,
    "early_stopping_patience": 10,

    "model":{
    	"lstm_hidden_size": 512,
   	    "lstm_num_layers": 1,
    	"convlstm_layers": [16,16,16],
        "dropout_classifier":0
    }
}
