{
    "gpus": 1,
    "epochs": 50,
    "batch_size": 1,
    "num_workers": 8,
    "use_joints": false,
    "early_stopping_patience": 5,

    "model":{
    	"lstm_hidden_size": 512,
   	    "lstm_num_layers": 1,
    	"convlstm_layers": [64],
        "dropout_classifier":0.1
    }
}
