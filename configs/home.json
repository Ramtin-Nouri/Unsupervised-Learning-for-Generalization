{
    "gpus": 1,
    "epochs": 10,
    "unsupervised_epochs": 25,
    "batch_size": 1,
    "num_workers": 8,
    "input_length": 15,
    "learning_rate": 0.001,
    "predict_ahead":2,

    "use_joints": false,
    "model":{
    	"convlstm_layers": [16,16,16,16],
        "convolution_layers_decoder": [16,16,16,16],
	    "dropout":0,
        "lstm_num_layers": 4,
        "lstm_hidden_size": 64
    },
    "dataset":{
        "num_training_samples_unsupervised": 250
    }
}
