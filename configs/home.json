{
    "gpus": 1,
    "epochs": 10,
    "unsupervised_epochs": 10,
    "batch_size": 1,
    "num_workers": 8,
    "input_length": 15,

    "use_joints": false,
    "model":{
        "convlstm_features":16,
        "convlstm_layers":2,
        "convolution_layers_decoder":[32],
        "lstm_num_layers": 4,
        "lstm_hidden_size": 64
    }
}