{
    "gpus": 1,
    "epochs": 50,
    "batch_size": 1,
    "num_workers": 8,
    "early_stopping_patience": 5,
    "predict_ahead":1,

    "use_joints": false,
    "model":{
    	"convlstm_layers": [16, 16, 16],
        "dropout_classifier":0.0,
        "lstm_num_layers": 1,
        "lstm_hidden_size": 512
    }
}
